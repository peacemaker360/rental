[project]
name = "association-rental-worker"
version = "0.1.0"
description = "Cloudflare Python Worker backend for an association instrument rental app."
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "workers-py>=1.14.0",
    "workers-runtime-sdk"
]

[tool.pytest.ini_options]
pythonpath = ["."]
